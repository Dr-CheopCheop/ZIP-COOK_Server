spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zipcook?serverTimezone=UTC&characterEncoding=UTF-8
    username: seunghee
    password: Leesh0127!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true

